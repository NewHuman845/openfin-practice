{
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "start-server-and-test": "^2.1.2",
    "wait-on": "^9.0.1"
  },
  "scripts": {
    "wait-openfin": "npx wait-on tcp:9091",
    "openfin-playwright": "npx openfin --launch --config ../openfinproject/openfin/app.json && npm run wait-openfin && npx playwright test",
    "e2e": "start-server-and-test \"npm --prefix ../openfinproject run serve\" http://localhost:4200 openfin-playwright"
  }
}
