{
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "start-server-and-test": "^2.1.2"
  },
  "scripts": {
    "openfin-playwright": "openfin --launch --config ../openfinproject/openfin/app.json && npx playwright test",
    "e2e": "start-server-and-test \"npm --prefix ../openfinproject run serve\" http://localhost:4200 openfin-playwright"
  }
}
